// SpiceDB schema with invalid syntax

definition user {}

definition group {
    relation organization: organization
    relation member: user | group#member
    relation invalid_relation: nonexistent_type  // Invalid: nonexistent_type doesn't exist

    permission edit_members = organization->administrate
}

definition organization {
    relation admin: user | group#member

    permission administrate = admin
}

// Invalid definition: missing closing brace
definition invalid_definition {
    relation test: user
